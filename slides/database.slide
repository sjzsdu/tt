---
title: 数据库设计
template: dark
---

# 数据库设计

订单系统核心表结构

---

# ER 图

```mermaid
erDiagram
    USER ||--o{ ORDER : places
    ORDER ||--|{ ORDER_ITEM : contains
    PRODUCT ||--o{ ORDER_ITEM : "referenced in"
    ORDER ||--o| PAYMENT : "paid by"
    USER {
        int id PK
        string name
        string email
        datetime created_at
    }
    ORDER {
        int id PK
        int user_id FK
        decimal total
        string status
        datetime created_at
    }
    ORDER_ITEM {
        int id PK
        int order_id FK
        int product_id FK
        int quantity
        decimal price
    }
    PRODUCT {
        int id PK
        string name
        decimal price
        int stock
    }
    PAYMENT {
        int id PK
        int order_id FK
        decimal amount
        string method
        datetime paid_at
    }
```

---

# 表结构

| 表名 | 说明 | 关键字段 |
|------|------|---------|
| `users` | 用户表 | id, name, email |
| `orders` | 订单表 | id, user_id, total, status |
| `order_items` | 订单项 | id, order_id, product_id, quantity |
| `products` | 商品表 | id, name, price, stock |
| `payments` | 支付表 | id, order_id, amount, method |

---

# 订单状态机

```mermaid
stateDiagram-v2
    [*] --> 待支付
    待支付 --> 已支付 : 支付成功
    待支付 --> 已取消 : 超时/取消
    已支付 --> 已发货 : 发货
    已发货 --> 已收货 : 确认收货
    已收货 --> 已完成 : 评价/完成
    已支付 --> 退款中 : 申请退款
    退款中 --> 已退款 : 退款成功
    已收货 --> 退款中 : 申请退款
```

---

# 数据流

```d2
shape: sequence_diagram
Client -> API: 创建订单
API -> OrderService: 创建订单
OrderService -> DB: INSERT order
OrderService -> DB: INSERT items
OrderService -> MQ: 发布事件
MQ -> InventoryService: 扣减库存
InventoryService -> DB: UPDATE stock
MQ -> NotifyService: 发送通知
NotifyService -> Client: 推送消息
```

---

# 索引策略

```sql
-- 订单表：按用户查询
CREATE INDEX idx_orders_user_id ON orders(user_id);

-- 订单表：按状态查询
CREATE INDEX idx_orders_status ON orders(status);

-- 订单项：按订单查询
CREATE INDEX idx_order_items_order_id ON order_items(order_id);

-- 复合索引：用户订单按时间
CREATE INDEX idx_orders_user_created 
ON orders(user_id, created_at DESC);
```

---

# 分库分表

:::columns
## 策略

- 按 `user_id` 哈希分片
- 16 个逻辑库
- 每库 16 张订单表

:::

:::columns
## 路由

```go
func GetShard(userID int64) int {
    return userID % 256
}

func GetTable(orderID int64) int {
    return orderID % 16
}
```

:::
