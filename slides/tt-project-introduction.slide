---
title: tt slide 能力示例集
transition: fade
---

.cover

# tt slide 能力示例集

用一个真实 Go CLI 项目，展示 `.slide` 能承载的典型演示页

`Cover` · `Center` · `Cards` · `Grid` · `Media` · `Diagram` · `Table` · `Code`

---

.grid

# 页面类型一览

:::item
## 叙事页
封面、中心句、hero、结束页，用来建立主题和收束观点。
:::

:::item
## 结构页
双栏、卡片、网格，用来拆解模块、能力、选项和差异。
:::

:::item
## 流程页
Mermaid / D2 图，用来表达架构、调用链和执行路径。
:::

:::item
## 证据页
表格、代码、图文页，用来承载数据、配置、截图和源码片段。
:::

---

.hero

# `.slide` 只写语义

同一份文档可以交给不同模板渲染；内容不绑定品牌、颜色、CSS 或屏幕尺寸。

- `---` 表达分页
- 点号指令表达页面角色
- Markdown / Mermaid / D2 表达内容

---

.center

# 先选页面角色，再写内容

一页只承担一个视觉任务：讲结论、做比较、展示结构、解释流程，或收束观点。

---

.cards

# 典型页面模式

:::card
## Cover / End
承载开场和结束，把主题、上下文和品牌呈现交给模板。
:::

:::card
## Center / Hero
承载一句关键判断，避免短内容被挤在页面左上角。
:::

:::card
## Columns
承载左右对比，例如问题/方案、现状/目标、文档层/视觉层。
:::

:::card
## Cards / Grid
承载 3 到 6 个并列对象，例如能力、模块、风险、方案。
:::

:::card
## Media / Diagram
承载图文并排、架构图、时序图、流程图和截图。
:::

:::card
## Table / Code
承载命令清单、配置片段、状态矩阵和示例源码。
:::

---

.two-column

# 文档层和视觉层分开

:::columns
## `.slide` 文档层

- 标题、正文、列表、表格
- 代码块和引用
- Mermaid / D2 图
- `.center`、`.grid`、`.cards` 等语义
- 相对路径业务图片
:::

:::columns
## Template 视觉层

- 字体、颜色、间距
- 封面和封底
- 卡片、网格、双栏的具体样式
- 背景、logo、装饰素材
- Reveal.js 默认参数
:::

---

.media-right

:::main
# 图文页：内容和证据并排

- `:::main` 承载解释文本
- `:::media` 承载图片、SVG 或截图
- 适合展示系统地图、产品界面、运行结果
- 业务图片使用相对路径，随 deck 一起移动
:::

:::media
![tt runtime map](./assets/tt-runtime-map.svg)
:::

---

.grid

# 真实项目素材：tt 的四类能力

:::item
## 本地内容工作台
`markdown`、`slide`、`json`、`conversation`、`skill`
:::

:::item
## Agent Runtime
`agent`、`translate`、`debate`、`nvwa`、`repo2skill`
:::

:::item
## Formula Workflow
`formula run`、`resume`、`input`、`runs`、`open`
:::

:::item
## 工程生成与理解
`cmd2skill`、`agent optimize`、`docs analyze`
:::

---

# 功能地图：用 mindmap 展示能力分组

```mermaid
mindmap
  root((tt))
    本地内容工作台
      markdown
      slide
      json
      conversation
      skill
    Agent Runtime
      agent
      translate
      debate
      nvwa
      repo2skill
    Formula Workflow
      formula
      dashboard
      resume
      human input
    工程生成与理解
      cmd2skill
      repo2skill
      agent optimize
    代码理解文档
      docs analyze
      ai-docs
```

---

.cards

# 本地内容工作台可以用卡片页展示

:::card
## Markdown
浏览、预览、编辑工作区里的 Markdown 文档。
:::

:::card
## Slide
打开 `.slide` deck，用 Reveal.js 播放并支持模板渲染。
:::

:::card
## JSON
查看和编辑结构化 JSON 文件，适合配置和运行状态。
:::

:::card
## Conversation
浏览会话型 JSON，把消息、角色和上下文分开呈现。
:::

:::card
## Skill
查看和编辑 agent skill 文档，让技能内容更容易维护。
:::

---

# 本地 Web UI 的调用链

```mermaid
sequenceDiagram
    participant User as 用户
    participant Cmd as cmd 层
    participant HTTP as 本地 HTTP 服务
    participant FS as 工作区文件系统
    participant UI as 嵌入式前端

    User->>Cmd: tt markdown / tt slide / tt json
    Cmd->>HTTP: 启动 localhost 服务
    Cmd->>UI: 提供 go:embed 静态资源
    UI->>HTTP: 请求文件列表和内容
    HTTP->>FS: 读取或写入本地文件
    HTTP-->>UI: 返回内容和状态
```

---

.two-column

# Agent 能力通过 Picoclaw 复用

:::columns
## 共享适配层

- 解析 tt 与 Picoclaw 配置
- 创建 provider 和 message bus
- 注册 embedded agents
- 暴露 DirectRunner 同步接口
:::

:::columns
## 被复用的命令

- `tt agent`
- `tt translate`
- `tt debate`
- `tt nvwa`
- `tt repo2skill`
- `tt docs analyze`
- `tt formula run` 的 agent step
:::

---

# DirectRunner 时序图

```mermaid
sequenceDiagram
    participant Cmd as 命令层
    participant RT as Runtime
    participant DR as DirectRunner
    participant AL as Agent Loop

    Cmd->>RT: NewDirectRunner(options)
    RT->>DR: 创建 provider 和 bus
    RT->>DR: 注册 embedded agents
    Cmd->>DR: ProcessDirect(message)
    DR->>RT: ResolveRunOptions
    DR->>AL: ProcessDirectForAgent
    AL-->>DR: response
    DR-->>Cmd: normalized response
```

---

# Formula Workflow：声明到执行

```mermaid
flowchart TD
    A[formula TOML / JSON] --> B[Parser]
    B --> C[Resolve extends / compose / advice]
    C --> D[Filter compile-time conditions]
    D --> E[AST to IR.Workflow]
    E --> F[Typed Runtime Executor]
    F --> P[Preflight checks]
    P --> W[Prepare workspace]
    W --> T[Plan topological order]
    T --> S{Step kind}
    S -->|agent| AG[AgentStep]
    S -->|script| SC[ScriptStep]
    S -->|human_input| HI[HumanInputStep]
    S -->|loop| LP[LoopStep]
    AG --> Store[Run Store]
    SC --> Store
    HI --> Store
    LP --> Store
    Store --> Dash[Dashboard]
```

---

.cards

# Dashboard 用来查看 run 的进度和产物

:::card
## 运行状态
workflow timeline、graph/list 视图、step 状态和 workspace ready 事件。
:::

:::card
## 人机协作
prompt / output / error、waiting input form、final report follow-up chat。
:::

:::card
## 自我修复
Repairs 面板展示 advice、fixed command、attempt 和人工确认状态。
:::

:::card
## 可恢复产物
run.json、workflow.json、state.json、logs.jsonl、steps/*、patches/*。
:::

---

.grid

# Step kind 适合用网格页速览

:::item
## agent
调用 Picoclaw agent，默认可重试。
:::

:::item
## script
执行确定性命令，默认非幂等。
:::

:::item
## human_input
等待用户在 CLI 或 Dashboard 中补充字段。
:::

:::item
## loop
对数组或固定次数执行嵌套 typed step。
:::

:::item
## retry
封装失败重试和耗尽策略。
:::

:::item
## write_files
把结构化输出落到工作区文件。
:::

---

# 表格页：命令和运行路径

| 类型 | 代表命令 | 运行方式 |
| --- | --- | --- |
| 本地 Web UI | `markdown` / `slide` / `json` | Go 本地 HTTP 服务 + 嵌入式 React 前端 |
| Agent 命令 | `agent` / `translate` / `debate` | Picoclaw DirectRunner |
| Formula 工作流 | `formula run` / `formula resume` | TOML → IR → typed runtime → run store |
| 工程生成工具 | `cmd2skill` / `repo2skill` | 结构化分析 + agent / fallback analyzer |
| 代码理解 | `docs analyze` | docs-analyst agent + Markdown 文档包 |

---

# 代码页：`.slide` 源码本身也能展示

```text
---
title: Demo Deck
transition: fade
---

.media-right

:::main
# 图文页标题

- 解释文本放在 main
- 图片、SVG 或截图放在 media
:::

:::media
![Runtime map](./assets/tt-runtime-map.svg)
:::
```

---

# D2 页：另一种轻量图表

```d2
shape: sequence_diagram
Presenter -> TTSlide: open deck.slide
TTSlide -> Parser: parse markdown and directives
Parser -> Renderer: semantic slide parts
Renderer -> Template: visual treatment
Template -> Audience: composed slide
```

---

.hero

# 一份 deck 可以同时是演示、文档和回归样例

示例页既展示了 `.slide` 的语义能力，也复用了项目里的真实架构素材；后续改模板时，可以用它快速检查常见页面是否都渲染正常。

---

.end
